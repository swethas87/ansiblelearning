- name: conditions
  hosts: web
  become: yes
  tasks:
    - name: check if user exists or not
      ansible.builtin.command: id expense
      ignore_errors: true
    - name: say hello
      ansible.builtin.debug:
        msg: "Hello"
